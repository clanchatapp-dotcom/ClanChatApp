# Test credentials

Seed accounts (alice/bob/teen/admin@clanchat.app) have been purged from the preview DB as of Feb 2026 per user request.

To re-seed for local testing, set `SEED_DEMO_DATA=1` in `backend/.env` and restart the backend.
